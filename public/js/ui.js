// public/js/ui.js
(() => {
  // No-op base to guarantee the file exists.
  // Place light client helpers here if needed.
  // Example: a quick console greet once per load.
  if (!window.__bl_ui_boot__) {
    window.__bl_ui_boot__ = true;
    // console.log('BookLantern UI booted');
  }
})();