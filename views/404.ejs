<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('./partials/head.ejs', {
    pageTitle: '404 - Page Not Found',
    pageDescription: 'The page you’re looking for could not be found on BookLantern.'
  }) %>
  <style>
    :root { --bg:#0b0d10; --text:#e6edf3; --muted:#9aa3ad; --card:#12161b; --accent:#7aa2ff; }
    *{box-sizing:border-box}
    body{margin:0;background:var(--bg);color:var(--text);font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial}
    .wrap{min-height:100vh;display:flex;flex-direction:column}
    .main{flex:1;display:flex;align-items:center;justify-content:center;padding:40px}
    .card{max-width:640px;width:100%;background:var(--card);border:1px solid #273142;border-radius:14px;padding:28px;box-shadow:0 8px 28px rgba(0,0,0,.35);text-align:center}
    h1{margin:0 0 12px 0;font-size:32px}
    p{margin:0 0 18px 0;color:var(--muted)}
    a.btn{display:inline-block;padding:10px 14px;border-radius:10px;border:1px solid #2a3340;background:#0f1319;color:#e6edf3;text-decoration:none}
    a.btn:hover{background:#111723}
  </style>
</head>
<body>
  <div class="wrap">
    <%- include('./partials/navbar.ejs') %>
    <main class="main">
      <div class="card">
        <h1>404 — Page Not Found</h1>
        <p>We couldn’t find that page. It may have moved or never existed.</p>
        <a class="btn" href="/">← Go to Home</a>
      </div>
    </main>
  </div>
</body>
</html>
