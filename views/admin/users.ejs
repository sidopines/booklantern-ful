<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head'); %>
  <title>Admin • Users</title>
  <style>
    .admin-wrap{max-width:980px;margin:24px auto 64px;}
    .card{border:1px solid #eee;border-radius:12px;padding:16px;background:#fff}
    .note{background:#eef7ff;border:1px solid #cfe7ff;padding:8px 10px;border-radius:8px}
  </style>
</head>
<body>
  <%- include('../partials/header'); %>

  <main class="admin-wrap">
    <h1>Admin • Users</h1>
    <% if (messages && (messages.error || messages.success || messages.info)) { %>
      <p class="note"><%= messages.error || messages.success || messages.info %></p>
    <% } %>

    <section class="card">
      <p>This is a placeholder. No user operations are wired yet.</p>
    </section>
  </main>

  <%- include('../partials/footer'); %>
</body>
</html>
