<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Dashboard | BookLantern</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f9f9f9;
      margin: 0;
      padding: 2rem;
    }
    h1 {
      color: #333;
    }
    .nav {
      margin-bottom: 2rem;
    }
    .nav a {
      margin-right: 1rem;
      text-decoration: none;
      color: #0077cc;
    }
    .nav a:hover {
      text-decoration: underline;
    }
    .favorites {
      margin-top: 2rem;
    }
    .favorites h2 {
      margin-bottom: 1rem;
    }
    .book {
      padding: 0.5rem 1rem;
      background: white;
      border-radius: 5px;
      margin-bottom: 0.5rem;
      box-shadow: 0 1px 2px rgba(0,0,0,0.1);
    }
    .book a {
      color: #000;
      text-decoration: none;
    }
    .book a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <h1>Welcome, <%= user.name %>!</h1>

  <div class="nav">
    <a href="/watch">🎬 Watch Videos</a>
    <a href="/read">📖 Read Books</a>
    <a href="/settings">⚙️ Settings</a>
    <a href="/logout">🚪 Logout</a>
  </div>

  <div class="favorites">
    <h2>❤️ My Favorite Books</h2>
    <% if (favorites && favorites.length > 0) { %>
      <% favorites.forEach(fav => { %>
        <div class="book">
          <a href="/read/book/<%= fav.book._id %>"><%= fav.book.title %></a>
        </div>
      <% }) %>
    <% } else { %>
      <p>You haven't added any books to favorites yet.</p>
    <% } %>
  </div>
</body>
</html>
