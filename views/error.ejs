<%- include('./partials/head', { pageTitle: 'Error - BookLantern' }) %>
<body>
  <%- include('./partials/navbar') %>

  <main class="stack center" style="--gap:32px; padding:40px 16px; text-align:center;">
    <h1 style="font-size:2rem; font-weight:700; color:var(--ink);">Something went wrong</h1>
    <p style="max-width:600px; margin:0 auto; color:var(--ink-2);">
      <%= typeof message !== 'undefined' ? message : 'An unexpected error occurred. Please try again later.' %>
    </p>
    <div>
      <a class="btn primary" href="/">← Back to Home</a>
    </div>
  </main>

  <%- include('./partials/footer') %>
</body>
</html>
