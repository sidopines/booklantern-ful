<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('./partials/header.ejs', {
    pageTitle: 'My Favorite Books',
    pageDescription: 'Browse the books you’ve favorited on BookLantern.'
  }) %>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      background: #f8f8f8;
      color: #333;
    }
    .wrap {
      max-width: 900px;
      margin: 2rem auto;
      padding: 1rem 2rem;
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    }
    .back {
      display: inline-block;
      margin: 1rem 2rem 0;
      font-size: 0.9rem;
      color: #0077cc;
      text-decoration: none;
    }
    .back:hover {
      text-decoration: underline;
    }
    h1 {
      margin-top: 0;
      font-size: 1.8rem;
    }
    .book-list {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 1.5rem;
      margin-top: 1rem;
    }
    .book-card {
      background: white;
      padding: 1rem;
      border-radius: 10px;
      box-shadow: 0 0 6px rgba(0, 0, 0, 0.1);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    .book-card h3 {
      font-size: 1.1rem;
      margin-bottom: 0.5rem;
    }
    .book-card a {
      color: #0077cc;
      text-decoration: none;
      font-weight: 500;
    }
    .book-card a:hover {
      text-decoration: underline;
    }
    .book-card form {
      margin-top: 1rem;
    }
    .book-card button {
      padding: 0.4rem 0.8rem;
      background: crimson;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    .book-card button:hover {
      background: darkred;
    }
  </style>
</head>
<body>
  <a href="javascript:history.back()" class="back">← Back</a>
  <div class="wrap">
    <h1>📚 My Favorite Books</h1>

    <% if (!favorites || favorites.length === 0) { %>
      <p>You haven’t added any books to your favorites yet.</p>
    <% } else { %>
      <div class="book-list">
        <% favorites.forEach(fav => { %>
          <div class="book-card">
            <div>
              <h3><%= fav.book.title %></h3>
              <a href="/read/book/<%= fav.book._id %>">📖 Read Book</a>
            </div>
            <form action="/favorite/<%= fav.book._id %>/remove" method="POST">
              <button type="submit">Remove</button>
            </form>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>
</body>
</html>
