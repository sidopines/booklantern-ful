<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('./partials/header.ejs', {
    pageTitle: pageTitle || 'Home',
    pageDescription: pageDescription || 'Discover free books and knowledge on BookLantern.'
  }) %>
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
    rel="stylesheet"
  />
  <style>
    body {
      margin: 0;
      font-family: 'Inter', sans-serif;
      background-color: #fdfcfb;
      color: #333;
    }
    .hero {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 70vh;
      text-align: center;
      padding: 2rem;
      background: linear-gradient(to right, #e3f2fd, #fce4ec);
    }
    .hero h1 {
      font-size: 3rem;
      margin-bottom: 1rem;
      line-height: 1.1;
    }
    .hero p {
      font-size: 1.15rem;
      max-width: 680px;
      margin: 0 auto;
    }
    .cta {
      margin-top: 2rem;
      display: inline-flex;
      gap: 0.75rem;
      flex-wrap: wrap;
      align-items: center;
      justify-content: center;
    }
    .btn {
      padding: 0.8rem 1.8rem;
      font-size: 1rem;
      background: #111;
      color: #fff;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
    }
    .btn:hover { background: #333; }
    .btn.alt {
      background: #fff;
      color: #111;
      border: 1px solid #ddd;
    }
    .features {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      padding: 4rem 2rem;
      gap: 2rem;
    }
    .feature-box {
      background: white;
      border-radius: 12px;
      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
      padding: 2rem;
      flex: 1;
      min-width: 280px;
      max-width: 400px;
      text-align: center;
    }
    .feature-box h3 { margin-bottom: 0.75rem; }
    footer {
      text-align: center;
      padding: 2rem;
      background: #111;
      color: white;
      margin-top: auto;
    }
  </style>
</head>
<body>
  <%- include('./partials/navbar.ejs') %>

  <section class="hero">
    <h1>Discover a World of Knowledge</h1>
    <p>
      Stream educational videos, read free books, and bookmark your favorites — all in one place,
      powered by open libraries like Archive.org and Project Gutenberg.
    </p>
    <div class="cta">
      <% if (user) { %>
        <a class="btn" href="/read">Start Reading</a>
        <a class="btn alt" href="/watch">Explore Videos</a>
      <% } else { %>
        <a class="btn" href="/register">Start Reading</a>
        <a class="btn alt" href="/login">Already have an account?</a>
      <% } %>
    </div>
  </section>

  <section class="features">
    <div class="feature-box">
      <h3>📖 Free Books</h3>
      <p>Access thousands of public-domain and open-license books — from classic literature to modern learning.</p>
    </div>
    <div class="feature-box">
      <h3>🎬 Watch Content</h3>
      <p>Enjoy educational videos without friction. Browse by genre and save favorites.</p>
    </div>
    <div class="feature-box">
      <h3>🔐 One Account, Full Access</h3>
      <p>Register once to unlock reading, watching, bookmarks, favorites, and more.</p>
    </div>
  </section>

  <footer>
    &copy; <%= new Date().getFullYear() %> BookLantern. Powered by open access and curiosity.
  </footer>
</body>
</html>
