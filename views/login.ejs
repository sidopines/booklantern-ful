<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login · BookLantern</title>
  <style>
    /* unobtrusive status panel for debugging */
    #auth-status {font-family: ui-sans-serif, system-ui, -apple-system; max-width: 680px; margin: 3rem auto; padding: 1rem 1.25rem; border: 1px solid rgba(0,0,0,.08); border-radius: 12px;}
    #auth-status .muted {opacity:.75; font-size:.9rem}
    #auth-status .error {color:#b00020; white-space:pre-wrap; word-break:break-word}
    #auth-status code {background: rgba(0,0,0,.04); padding: .1rem .3rem; border-radius: 6px}
  </style>
</head>
<body>
  <%- include('partials/navbar') %>

  <main>
    <section id="auth-status">
      <h1>Sign in</h1>
      <p class="muted">If you clicked a magic link, this page will complete your sign-in automatically.</p>
      <div id="auth-progress" class="muted">Waiting for tokens…</div>
      <pre id="auth-error" class="error" style="display:none"></pre>
      <noscript><p class="error">JavaScript is required to sign in.</p></noscript>
    </section>
  </main>

  <%- include('partials/footer') %>

  <!-- Config from server env (make sure these locals are set in app.js) -->
  <script>
    window.__SUPABASE_URL__ = "<%= typeof SUPABASE_URL !== 'undefined' ? SUPABASE_URL : (process.env.SUPABASE_URL || '') %>";
    window.__SUPABASE_ANON_KEY__ = "<%= typeof SUPABASE_ANON_KEY !== 'undefined' ? SUPABASE_ANON_KEY : (process.env.SUPABASE_ANON_KEY || '') %>";
    window.__LOGIN_REDIRECT_DEFAULT__ = "/dashboard";
  </script>

  <!-- Auth callback handler -->
  <script type="module" src="/public/js/auth-callback.js"></script>
</body>
</html>
