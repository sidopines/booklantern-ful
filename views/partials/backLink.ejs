<%
// Safe default so this partial NEVER throws if `referrer` isn't provided.
const _ref = (typeof referrer !== 'undefined' && referrer) ? referrer : '/';
%>
<a class="back" href="<%= _ref %>"
   onclick="if(document.referrer){ history.back(); return false; }">
  ← Back
</a>
