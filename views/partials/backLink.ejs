<%
// Safe back link: works even if 'referrer' was never passed.
const href =
  (typeof referrer !== 'undefined' && referrer && typeof referrer === 'string')
    ? referrer
    : '/';
%>
<a class="btn btn--ghost" href="<%= href %>">← Back to Home</a>
