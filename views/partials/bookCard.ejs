<% const item = b || {}; %>
<article class="card">
  <a class="cover" href="/read/<%= item.provider %>/<%= item.id %>">
    <% if (item.cover) { %>
      <img src="<%= item.cover %>" alt="<%= item.title %>">
    <% } else { %>
      <div class="cover ph" aria-label="No cover">📘</div>
    <% } %>
  </a>
  <div class="meta">
    <h4 class="title"><%= item.title %></h4>
    <% if (item.author) { %><div class="author"><%= item.author %></div><% } %>
    <div class="row" style="gap:6px;margin-top:8px;">
      <a class="btn tiny" href="/read/<%= item.provider %>/<%= item.id %>">Read</a>
      <a class="btn tiny ghost" href="/read/<%= item.provider %>/<%= item.id %>#listen">Listen</a>
    </div>
  </div>
</article>
