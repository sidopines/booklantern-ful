<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title><%= typeof pageTitle !== 'undefined' ? pageTitle + ' · ' : '' %>BookLantern</title>

  <!-- Favicons / PWA -->
  <link rel="icon" href="/public/favicon.ico" sizes="any">
  <link rel="icon" type="image/svg+xml" href="/public/favicon.svg">
  <link rel="apple-touch-icon" href="/public/apple-touch-icon.png">
  <link rel="manifest" href="/public/site.webmanifest">

  <!-- Early theme bootstrap: set data-theme before paint -->
  <script>
    (function () {
      try {
        var KEY = 'bl-theme';
        var saved = localStorage.getItem(KEY);
        var theme = saved || (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
        document.documentElement.setAttribute('data-theme', theme);
      } catch (_) {}
    })();
  </script>

  <!-- OPTIONAL: if you will self-host “Soria” later, put your @font-face in site.css -->
  <!-- Until then, we use the family name with fallbacks so design stays consistent. -->

  <link rel="stylesheet" href="/public/css/site.css?v=<%= buildId %>" />
</head>
