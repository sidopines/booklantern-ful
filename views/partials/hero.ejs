<!-- views/partials/hero.ejs - Homepage hero with headline, subhead, single search -->
<section class="hero">
  <div class="hero-container">
    <h1 class="hero-headline">Welcome to the largest hub of free books.</h1>
    <p class="hero-subhead">Millions of books from globally trusted libraries. One search, one clean reader.</p>
    
    <form class="hero-search" action="/read" method="GET">
      <div class="hero-search-wrapper">
        <svg class="hero-search-icon" width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <circle cx="11" cy="11" r="8"></circle>
          <path d="m21 21-4.35-4.35"></path>
        </svg>
        <input 
          type="text" 
          name="query" 
          class="hero-search-input" 
          placeholder="Search millions of books..."
          aria-label="Search books"
        >
        <button type="submit" class="hero-search-button">Search</button>
      </div>
    </form>
  </div>
</section>

<style>
/* Hero Styles */
.hero {
  background: linear-gradient(135deg, rgba(37,99,235,.10), rgba(124,58,237,.10));
  padding: 80px 0 100px;
  text-align: center;
}

.hero-container {
  max-width: var(--container);
  margin: 0 auto;
  padding: 0 24px;
}

.hero-headline {
  font-size: var(--h1);
  font-weight: 800;
  color: var(--ink);
  margin: 0 0 24px;
  line-height: 1.1;
  max-width: 800px;
  margin-left: auto;
  margin-right: auto;
}

.hero-subhead {
  font-size: var(--h3);
  color: var(--ink-weak);
  margin: 0 0 48px;
  line-height: 1.4;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

.hero-search {
  max-width: 600px;
  margin: 0 auto;
}

.hero-search-wrapper {
  position: relative;
  display: flex;
  align-items: center;
  background: var(--surface);
  border: 2px solid var(--line);
  border-radius: 12px;
  box-shadow: var(--shadow);
  transition: all 0.2s ease;
}

.hero-search-wrapper:focus-within {
  border-color: var(--brand);
  box-shadow: var(--shadow-lg), 0 0 0 3px var(--ring);
}

.hero-search-icon {
  position: absolute;
  left: 16px;
  color: var(--ink-weak);
  pointer-events: none;
}

.hero-search-input {
  flex: 1;
  border: none;
  background: transparent;
  padding: 16px 16px 16px 48px;
  font-size: 16px;
  color: var(--ink);
  outline: none;
}

.hero-search-input::placeholder {
  color: var(--ink-weak);
}

.hero-search-button {
  background: var(--brand);
  color: var(--brand-ink);
  border: none;
  padding: 12px 24px;
  margin: 4px;
  border-radius: 8px;
  font-weight: 600;
  font-size: 15px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.hero-search-button:hover {
  background: var(--accent);
  transform: translateY(-1px);
}

.hero-search-button:focus {
  outline: 2px solid var(--ring);
  outline-offset: 2px;
}

/* Mobile Responsive */
@media (max-width: 768px) {
  .hero {
    padding: 60px 0 80px;
  }
  
  .hero-search-wrapper {
    flex-direction: column;
    align-items: stretch;
  }
  
  .hero-search-icon {
    position: static;
    align-self: flex-start;
    margin: 16px 16px 0;
  }
  
  .hero-search-input {
    padding: 16px;
  }
  
  .hero-search-button {
    margin: 8px;
    padding: 16px;
    border-radius: 8px;
  }
}
</style>
