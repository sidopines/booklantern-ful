<nav class="navbar">
  <div class="container bar">
    <a href="/" class="brand" aria-label="BookLantern home">
      <span class="dot" aria-hidden="true"></span>
      <img src="/public/logo.svg" alt="" width="20" height="20" style="vertical-align:middle;">
      <span>BookLantern</span>
    </a>

    <div class="nav-links">
      <a href="/#browse">Browse</a>
      <a href="/read">Read</a>
      <a href="/watch">Watch</a>
      <a href="/about">About</a>
      <a href="/contact">Contact</a>
    </div>

    <div class="nav-right">
      <!-- Theme toggle cycles: Light → Dark → System -->
      <button id="themeToggle" class="btn tiny" aria-label="Toggle theme" data-mode="system">Auto</button>

      <% if (isAuthenticated) { %>
        <a class="btn tiny ghost" href="<%= isAdmin ? '/admin' : '/dashboard' %>">Account</a>
        <form method="post" action="/logout" style="display:inline;">
          <button class="btn tiny" type="submit">Logout</button>
        </form>
      <% } else { %>
        <a class="btn tiny ghost" href="/login">Login</a>
        <a class="btn tiny" href="/register">Sign up</a>
      <% } %>
    </div>
  </div>
</nav>
