<nav class="navbar">
  <div class="container navbar-inner">
    <div class="nav-left">
      <a class="brand" href="/">
        <img src="/public/logo.svg" alt="BookLantern logo" height="28" width="28">
        <span>BookLantern</span>
      </a>
    </div>

    <form class="search-wrap" action="/search" method="get" role="search" style="flex:1;">
      <input type="search" name="q" placeholder="Search free books…" aria-label="Search books" value="<%= typeof q !== 'undefined' ? q : '' %>">
      <button class="btn primary" type="submit">Search</button>
    </form>

    <div class="nav-right">
      <!-- Theme toggle -->
      <button id="themeToggle" class="theme-toggle" type="button" aria-pressed="false" title="Toggle theme">
        <span aria-hidden="true">🌙</span><span class="sr-only">Toggle theme</span>
      </button>

      <% if (loggedIn) { %>
        <a href="<%= isAdmin ? '/admin' : '/dashboard' %>">Account</a>
        <form method="POST" action="/logout" style="display:inline;">
          <button class="btn ghost" type="submit">Logout</button>
        </form>
      <% } else { %>
        <a href="/login">Login</a>
        <a class="btn" href="/register">Create account</a>
      <% } %>
    </div>
  </div>
</nav>
