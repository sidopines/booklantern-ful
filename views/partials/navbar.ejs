<header class="site-header">
  <div class="container nav-row">
    <a class="brand" href="/">
      <img src="/logo.svg" alt="BookLantern" width="44" height="44" class="brand-logo">
      <span class="brand-name">BookLantern</span>
    </a>

    <nav class="nav-links">
      <a href="/read/pg/10471" class="nav-link">Read</a>
      <a href="/watch" class="nav-link">Watch</a>
      <a href="/about" class="nav-link">About</a>
      <a href="/contact" class="nav-link">Contact</a>
    </nav>

    <form class="nav-search" action="/search" method="get" role="search">
      <input name="q" type="search" placeholder="Search free books…" aria-label="Search books" value="<%= (typeof q !== 'undefined' ? q : '') %>">
      <button class="btn">Search</button>
    </form>

    <div class="nav-actions">
      <button id="themeToggle" class="btn ghost" type="button" aria-label="Toggle theme">🌙</button>

      <% if (isAuthenticated) { %>
        <a class="btn ghost" href="/dashboard">Account</a>
        <form method="post" action="/logout" style="display:inline;">
          <button class="btn" type="submit">Logout</button>
        </form>
      <% } else { %>
        <a class="btn ghost" href="/login">Login</a>
        <a class="btn primary" href="/register">Create account</a>
      <% } %>
    </div>
  </div>
</header>
