<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head', { pageTitle: title }) %>
  <body class="page">
    <%- include('partials/nav') %>

    <main class="container" style="max-width: 1100px;">
      <h1 class="title"><%= title %></h1>

      <div class="card" style="padding:0; overflow:hidden;">
        <% if (isEmbeddable && embedUrl) { %>
          <div style="position:relative; padding-bottom:56.25%; height:0; overflow:hidden; background:#eee;">
            <iframe
              src="<%= embedUrl %>"
              title="<%= title %>"
              frameborder="0"
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
              allowfullscreen
              style="position:absolute; top:0; left:0; width:100%; height:100%; border:0;">
            </iframe>
          </div>
        <% } else { %>
          <div class="p-4">
            <p>We couldn’t embed this video automatically.</p>
            <% if (url) { %>
              <p>
                You can watch it directly here:
                <a href="<%= url %>" target="_blank" rel="noopener">Open original video</a>
              </p>
            <% } %>
          </div>
        <% } %>
      </div>

      <div class="mt-3">
        <a href="/watch">← Back to Watch</a>
      </div>
    </main>

    <%- include('partials/footer') %>
    <script src="/public/js/theme.js?v=<%= Date.now() %>"></script>
  </body>
</html>
